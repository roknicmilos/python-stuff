def sequence_generator(sequence):
    """
    GENERATOR FUNCTION that returns GENERATOR ITERATOR.
    Allows to create iterators using functional approach (style).
    """
    for item in sequence:
        yield item
