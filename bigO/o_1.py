len(range(1, 10))

"""
If we increase the range to 10x more than the previous range,
the time it takes to calculate the length using len(range(...)) 
will remain the same.

This is called O(1) or CONSTANT TIME COMPLEXITY.
"""
